0 пн
intro

1
Що таке змінна
Як створити змінну
примітивні типи даних
виведення стану
typeof
Операції зі стрінгами
Операції з числами
Операції з булевими типами

Конвертація стрінги до числа
Конвертація стрінги до булевого
Конвертація числа до стрінги
Конвертація числа до булевого




 2
Як запакувати купу занчень в одну змінну
Звернення до індексів масиву
Перевизначення елементів масиву
масив в масиві

Як описати сутність в програмуванні
створення об'єкту
додавання характеристик два способи
завернення до характеристик
вбудований об'єкт
об'єкт з масивом

масив об'єктів

Різниця примітиву від ссилочного типу



3
Логічні розгалуження для чого?
if
else
else if
множинні умови || &&
switch
Коли краще використовувати і що
Тренарний оператор як заміна if
Дефолтні значення змнних
let x = null || 'default'


4
Цикл, для того,щоб побудувати н-кількість блоків через write
for
Цикл для роботи з масивом та довжиною масиву
Цикл фор оф
wile
do while
Ітерація об'єкту та динамічна побудова на основі полів об'єкту


5
Функції для чого вони
Функції з return
Функції без ретерну (для роботи з ссилочними типами,приклад фільтрація масиву)
Аргумент  та рест аргумент
функція в функції
Функціональний вираз, та відмінність від звичайної (одразу з сплиттям)
Функції в об'єктах
selfinvoke



6
String methods
   String.prototype.concat()
   String.prototype.toLowerCase()
   String.prototype.toUpperCase()
   String.prototype.endsWith()
   String.prototype.includes()
   String.prototype.startsWith()
   String.prototype.substring()
   String.prototype.indexOf()
   String.prototype.charAt()
   String.prototype.lastIndexOf()
   String.prototype.replace()
   String.prototype.replaceAll()
   String.prototype.split()


array methods
        Array.isArray()
        Array.prototype.push()
        Array.prototype.pop()
        Array.prototype.shift()
        Array.prototype.unshift()
        Array.prototype.join()
        Array.prototype.concat()
        Array.prototype.reverse()
        Array.prototype.splice()
        Array.prototype.slice()
        Array.prototype.includes()

        Array.prototype.forEach()
        Array.prototype.filter()
        Array.prototype.map()
        Array.prototype.find()
        Array.prototype.every()
        Array.prototype.some()

        Array.prototype.sort()

        Array.prototype.reduce()

 створення свого колбеку

7
try catch

OChainO .? Оператор опціональної почлідовності

створення об'єкту
деструктуризація об'єкта
деструктуризація масиву

Копіювання об'єкта
Копіювання об'єкта поверхневе shallow
Копіювання об'єкта повне. Deep copy - JSON

Замикання
condtructors & class call apply bind prototype


8
try catch
DOM bytag, byclass, id,
setAttribute getAtribute
classlist
child,
querySelector
formsAPI
create + append / append child
рекурсия в дом
clicck...event
preventDefault
object taget
this reference
forms+events
filtering,sorting array
regexp
рекурсия

9
BOM localstorage + url + position + streams
fetch get post methods users/:id/posts работа с различными api

10 пт
setTimeout setInterval Async promises eloop

11
ts

12
ts






